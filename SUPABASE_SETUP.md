# إعداد Supabase

## مشروع جديد كلياً (لم يُشغَّل فيه أي شيء بعد)

1. افتح [supabase.com](https://supabase.com) وسجّل الدخول أو أنشئ حساباً.
2. اضغط **New Project** واختر اسماً وكلمة مرور لقاعدة البيانات ومنطقة قريبة.
3. من **SQL Editor > New query**، انسخ محتوى [supabase/schema.sql](supabase/schema.sql) كاملاً والصقه واضغط **Run**.
   هذا الملف يحتوي على كل شيء دفعة واحدة (الجداول، RLS، الأقسام، المهام، حاويات التخزين) — لا حاجة لتشغيل أي ملف من `migrations/` بعده.
4. من **Project Settings > API**: انسخ **Project URL** إلى `VITE_SUPABASE_URL`، و**anon public key** إلى `VITE_SUPABASE_ANON_KEY` (بملف `app/.env`، انسخه من `.env.example`).
5. شغّل `npm run dev`.

## مشروع قائم بالفعل (عنده بيانات حقيقية)

كل تحديث بالسكيما بعدها له ملف مستقل بمجلد [supabase/migrations/](supabase/migrations/)، **بالترتيب الرقمي**، يُشغَّل مرة واحدة فقط لكل ملف:

| الملف | الوصف |
|---|---|
| `0001_fix_company_insert_rls.sql` | إصلاح صلاحية إنشاء الشركة الأولى |
| `0002_print_settings.sql` | إعدادات طباعة التقارير (ترويسة الشركة) |
| `0003_project_status.sql` | حالة المشروع (تحت التجهيز/قائم/منتهٍ) |
| `0004_header_color.sql` | لون رأس الصفحة القابل للتخصيص |
| `0005_org_structure.sql` | حسابات حقيقية، الأدوار، الأقسام، الدعوات |
| `0006_tasks.sql` | نظام المهام العام |

**قاعدة بسيطة**: افتح `supabase/migrations/`، وشغّل فقط الملفات اللي رقمها **أعلى** من آخر ملف شغّلته سابقاً، كل ملف لحاله بترتيبه، من SQL Editor بنفس طريقة الخطوة 3 أعلاه.

## المصادقة (Auth)

التطبيق يستخدم حسابات حقيقية (بريد إلكتروني + كلمة مرور) — لا حاجة لتفعيل أي خيار إضافي بلوحة Supabase.

بشكل افتراضي، Supabase يفرض حد منخفض جداً لإرسال إيميلات التأكيد (عدة إيميلات بالساعة لكل المشروع). هذا كافٍ للاستخدام العادي (دخولك + دعوة عضو بين فترة وأخرى)، لكن لو احتجت حداً أعلى لاحقاً (مثلاً بعد شراء نطاق للشركة)، اربط مزوّد بريد خارجي من **Authentication > SMTP Settings**.

## ملاحظة أمنية

RLS مفعّل على كل الجداول ومبني على `company_id` — كل مستخدم يرى فقط بيانات شركته. حاويات التخزين خاصة (private) ويُصل لها عبر روابط موقّعة (signed URLs) من التطبيق فقط.
