# نشر التحديثات (Netlify Drop)

## في كل مرة تحتاج تحدّث الموقع المباشر

**1. ابنِ نسخة الإنتاج** (من مجلد `app/`):

```bash
npm run build
```

هذا يحدّث مجلد `app/dist` بآخر نسخة.

**2. حدّث نفس الموقع (بدون ما يتغيّر الرابط)**:

1. سجّل دخول على [app.netlify.com](https://app.netlify.com)
2. من **Sites**، افتح موقعك الحالي (**ليس** رابط `/drop` العام — هذا يسوي موقع جديد كل مرة)
3. من تبويب **Deploys**، اسحب مجلد `app/dist` بالكامل وأفلته بمربع الرفع بنفس الصفحة
4. انتظر اكتمال الرفع — نفس الرابط يتحدّث تلقائياً

## أول مرة فقط (موقع جديد كلياً)

1. افتح [app.netlify.com/drop](https://app.netlify.com/drop)
2. اسحب مجلد `app/dist` وأفلته
3. بعد الرفع، اضغط **Claim your site** (أو من داخل حسابك) لربطه بحسابك — بدون هالخطوة، أي تحديث لاحق يسوي موقع جديد بدل ما يحدّث نفس الرابط

## حذف مواقع زائدة

من **Sites**، افتح الموقع الزائد ← **Site configuration** ← **Danger zone** ← **Delete this site**.
⚠️ إجراء نهائي لا يمكن التراجع عنه — تأكد من اسم الموقع قبل الحذف.
